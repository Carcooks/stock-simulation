name: Update Stock Prices
on:
  schedule:
    - cron: '0 0 9,13,17 * * *'
jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - run: curl -X POST https://stock-simulator-rbf0fffmx-carcooks-projects.vercel.app/
